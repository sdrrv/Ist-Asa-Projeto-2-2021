#include <cstddef>
#include <iostream>
#include <memory>
#include <vector>
#include <list>
#include <stack>
#include <string>

struct Vertex{
  int h, e,
}

std::vector<int> h;
std::vector<int> e;
std::vector<std::list>
